# UCF Projection Lab

- 使用 C++20 和 CMake。
- 修改 C++ 后运行 `cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure`。
- 默认程序必须不带参数直接运行。
- 数据输入默认使用仓库内合成帧，README 必须同步记录验证命令与边界。
