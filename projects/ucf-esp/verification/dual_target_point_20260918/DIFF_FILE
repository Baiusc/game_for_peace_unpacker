diff --git a/projects/ucf-esp/verification/dual_target_point_20260918/ORIGINAL_FILE b/projects/ucf-esp/verification/dual_target_point_20260918/MODIFIED_FILE
index 0f75df4..acafa59 100755
--- a/projects/ucf-esp/verification/dual_target_point_20260918/ORIGINAL_FILE
+++ b/projects/ucf-esp/verification/dual_target_point_20260918/MODIFIED_FILE
@@ -203,6 +203,43 @@ static void record_frame(const ucf::Frame& f) {
     ++g_recorded_frames;
 }

+static int aim_bone_index(const ucf::Settings& settings) {
+    if (settings.aim_point_mode == 1) return 10; // Head
+    if (settings.aim_point_mode == 2) return 8;  // Chest
+    if (settings.aim_point_mode == 3) return settings.aim_bone_id;
+    return -1;
+}
+
+static bool aim_point_world(const ucf::Settings& settings, const ucf::PlayerState& player,
+                            ucf::Vec3& out) {
+    const int bone = aim_bone_index(settings);
+    if (bone < 0) {
+        out = {player.pos[0], player.pos[1], player.pos[2]};
+        return true;
+    }
+    if (bone >= ucf::MAX_BONES || !player.bones[bone].valid) return false;
+    out = {player.bones[bone].pos[0], player.bones[bone].pos[1], player.bones[bone].pos[2]};
+    return true;
+}
+
+static bool aim_point_screen(const ucf::Settings& settings, const ucf::ScreenMark& mark,
+                             float& x, float& y) {
+    const int bone = aim_bone_index(settings);
+    if (bone < 0) {
+        if (!mark.on_screen) return false;
+        x = mark.sx; y = mark.sy;
+        return true;
+    }
+    if (bone >= ucf::MAX_BONES || !mark.bones[bone].valid) return false;
+    x = mark.bones[bone].sx; y = mark.bones[bone].sy;
+    return true;
+}
+
+// 当前投影约定使用 OpenGL 风格的相机空间 -Z 为前方。
+static ucf::Vec3 camera_forward_from_view(const float w2c[16]) {
+    return {-w2c[2], -w2c[6], -w2c[10]};
+}
+
 static void cleanup_render_target() {
     if (g_rtv) { g_rtv->Release(); g_rtv = nullptr; }
 }
@@ -434,19 +471,24 @@ static void frame() {
         const int player_count = std::max(0, std::min(f.playerCount, ucf::MAX_PLAYERS));
         for (int i = 0; i < player_count; ++i) {
             const auto& p = f.players[i];
-            const float dx = p.pos[0] - f.local.pos[0];
-            const float dy = p.pos[1] - f.local.pos[1];
-            const float dz = p.pos[2] - f.local.pos[2];
+            ucf::Vec3 aim_point{};
+            const bool point_world_valid = aim_point_world(g_settings, p, aim_point);
+            float screen_x = 0.0f, screen_y = 0.0f;
+            const bool point_screen_valid = aim_point_screen(g_settings, g_marks[i + 1], screen_x, screen_y);
+            const float dx = aim_point.x - f.local.pos[0];
+            const float dy = aim_point.y - f.local.pos[1];
+            const float dz = aim_point.z - f.local.pos[2];
             candidates[i].dir = {dx, dy, dz};
             candidates[i].dist = std::sqrt(dx * dx + dy * dy + dz * dz);
             candidates[i].hp = p.hp;
             candidates[i].team = p.team;
             candidates[i].dead = p.isDead || p.hp <= 0;
-            candidates[i].visible = g_marks[i + 1].on_screen;
-            screen_candidates[i].x = g_marks[i + 1].sx;
-            screen_candidates[i].y = g_marks[i + 1].sy;
-            screen_candidates[i].valid = true;
-            if ((!g_settings.aim_teammates && p.team == f.local.team) ||
+            candidates[i].visible = point_screen_valid;
+            screen_candidates[i].x = screen_x;
+            screen_candidates[i].y = screen_y;
+            screen_candidates[i].valid = point_world_valid && point_screen_valid;
+            if (!point_world_valid ||
+                (!g_settings.aim_teammates && p.team == f.local.team) ||
                 (!g_settings.aim_dead && candidates[i].dead) ||
                 (g_settings.aim_visible_only && !candidates[i].visible) ||
                 (g_settings.aim_max_distance > 0.0f && candidates[i].dist > g_settings.aim_max_distance)) {
@@ -456,9 +498,14 @@ static void frame() {
         }
         const float fov = std::max(1.0f, std::min(180.0f, g_settings.fov_deg));
         const float fov_radius = (fov / 90.0f) * (std::min(float(f.width), float(f.height)) * 0.5f);
-        target = ucf::select_screen_target(screen_candidates, player_count,
-                                           float(f.width) * 0.5f, float(f.height) * 0.5f,
-                                           fov_radius);
+        if (g_settings.aim_selection_mode == 1) {
+            target = ucf::select_target(camera_forward_from_view(f.w2c), candidates,
+                                        player_count, g_settings.fov_deg, 2);
+        } else {
+            target = ucf::select_screen_target(screen_candidates, player_count,
+                                               float(f.width) * 0.5f, float(f.height) * 0.5f,
+                                               fov_radius);
+        }
         if (target >= 0) {
             output_angles = ucf::smooth_angles(output_angles,
                 ucf::angles_from_direction(candidates[target].dir),
@@ -537,10 +584,13 @@ static void frame() {
     st.target = target;
     st.target_yaw = output_angles.yaw;
     st.target_pitch = output_angles.pitch;
-    st.target_delta = (target >= 0 && target < f.playerCount && target < ucf::MAX_PLAYERS) ? ucf::angle_distance(output_angles,
-        ucf::angles_from_direction({f.players[target].pos[0] - f.local.pos[0],
-                                    f.players[target].pos[1] - f.local.pos[1],
-                                    f.players[target].pos[2] - f.local.pos[2]})) : 0.0f;
+    ucf::Vec3 selected_point{};
+    const bool selected_point_valid = target >= 0 && target < f.playerCount &&
+        target < ucf::MAX_PLAYERS && aim_point_world(g_settings, f.players[target], selected_point);
+    st.target_delta = selected_point_valid ? ucf::angle_distance(output_angles,
+        ucf::angles_from_direction({selected_point.x - f.local.pos[0],
+                                    selected_point.y - f.local.pos[1],
+                                    selected_point.z - f.local.pos[2]})) : 0.0f;
     st.bones_total = n * ucf::MAX_BONES;
     st.bones_valid = 0;
     for (int i = 0; i < n; ++i)
